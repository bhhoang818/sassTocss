section.section.banner-promotion
    .swiper-container
        .swiper-wrapper
            -for(i=1 ; i<3; i++)
                .swiper-slide
                    .slide-inner
                        .slide-inner__img
                            img.desktop(src="./images/banner-"+i+".jpg", alt="")
                            img.mobile(src="./images/promotion.png", alt="")
                            a.btn-detail(href="") 
                                p Chi tiết
    .swiper-button-next
    .swiper-button-prev
    .swiper-pagination